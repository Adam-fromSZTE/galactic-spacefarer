# Local development: prints the message without sending an email.
MAIL_MODE=console

# To deliver emails, change MAIL_MODE to smtp and supply your provider's settings.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
MAIL_FROM="Galactic Mission Control <mission-control@example.com>"
